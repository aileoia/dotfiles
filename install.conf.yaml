# Installation script for dotbot
- defaults:
    link:
      relink: true
      create: true
- link:
    ~/.antigen:
    ~/.vimrc:
    ~/.gitignore:
    ~/.vim/autoload/plug.vim: vim-plug/plug.vim
    ~/.zshrc:
